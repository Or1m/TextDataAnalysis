namespace Preprocess
{
    public class Tests
    {
        private void A()
        {
            //for (int i = 0; i<filteredWords.Count; i++)
            //{
            //    var item = filteredWords[i];
            //    if (item.Length == 1 && !char.IsDigit(item[0]) && item != "i")
            //    {

            //        Console.WriteLine($"{filteredWords[i - 1]} + {item}");
            //    }
            //}
            //return;
        }
        private void AA()
        {
            //for (int i = 1; i < words.Count; i++)
            //{
            //    if (words[i].Length == 1 && words[i] != "i" && words[i] != "a" && !char.IsDigit(words[i][0]))
            //        Console.WriteLine($"{words[i - 1]} + {words[i]}");

            //    if (words[i].Contains('\''))
            //        Console.WriteLine($"{words[i]}");
            //}
            //return;
        }
        private void B()
        {
            //for (int i = 0; i < filteredWords.Count; i++)
            //{
            //    if (filteredWords[i].Any(char.IsDigit) && !filteredWords[i].All(char.IsDigit))
            //        Console.WriteLine(filteredWords[i]);
            //}
        }
        private void BB()
        {
            //for (int i = 0; i < filteredWords.Count; i++)
            //{
            //    var item = filteredWords[i];
            //    if (item.Length == 1 && !char.IsDigit(item[0]) && item != "i" && item != "s")
            //    {

            //        Console.WriteLine($"{filteredWords[i - 1]} + {item}");
            //    }
            //}
            //return;
        }
    }
}
